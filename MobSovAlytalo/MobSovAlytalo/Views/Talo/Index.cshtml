@model IEnumerable<MobSovAlytalo.Models.Talo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Huone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Huone)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HuoneID }) |
            @Html.ActionLink("Details", "Details", new { id=item.HuoneID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HuoneID })
        </td>
    </tr>
}

</table>

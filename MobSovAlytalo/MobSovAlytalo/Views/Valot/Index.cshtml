@model IEnumerable<MobSovAlytalo.Models.Valo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    <button>@Html.ActionLink("Create New", "Create")</button> 
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ValonTila)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValonMaara)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Huone)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ValonTila)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValonMaara)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Huone)
        </td>
        <td>
            <button> @Html.ActionLink("Edit", "Edit", new { id = item.ValoID })</button> |
            <button>@Html.ActionLink("Details", "Details", new { id = item.ValoID })</button> |
            <button>@Html.ActionLink("Delete", "Delete", new { id = item.ValoID })</button> 
        </td>
    </tr>
}

</table>
